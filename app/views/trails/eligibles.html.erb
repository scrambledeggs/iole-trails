<h2>People eligible for <%= @trail.name.capitalize %> Trail</h2>
<ul>
  <% @trail.eligible_people.each do |person| %>
    <li>
      <%= link_to person.name.capitalize, person %>
    </li>
  <% end %>
</ul>

<br>
<%= link_to "Back", trail_path(@trail) %>